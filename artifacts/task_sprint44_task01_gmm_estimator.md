# Sprint 44 Task 1 — GMM estimator

Implemented `GMMConfig` and deterministic `GaussianMixtureDensity.fit()` with covariance selection, regularization, initialization, and dimension/sample constraints. Covered by `tests/test_density.py`.
