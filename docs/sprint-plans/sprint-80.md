# Sprint 80 Plan

**Status: Complete (bounded ordinary-DL core);** final sprint-wide differential review `agent://DeepReviewSprint80Fifth` — PASS. The blocked secondary SmolVLA lane and Sprint 81 release gates are excluded.

## Sprint Goal

Deliver and prove one trustworthy representation-diagnostic workflow for ordinary deep-learning models: capture internal representations, detect and localize defects, test explanations with statistical controls, validate causality through intervention, compare runs, and emit a reproducible engineering report.

## Product Claim

An AI engineer can start with a supported PyTorch model plus data and obtain a defensible diagnosis that states what failed, where it failed, how strong the evidence is, whether intervention supports the explanation, what changed between runs, and what remains unknown.

The supported core is proved on one encoder/autoencoder and one transformer hidden-state case. SmolVLA is a secondary, non-blocking portability lane under the permanent 16 GiB ceiling. Model-family count and historical theory-coverage percentages are not stable-release gates.

## Entry State

- `0.9.0` is published and verified through the GitHub Release asset contract.
- `main` contains the depth-first 1.0 contract and the Sprint 80 / Sprint 81 split.
- Historical Sprint 79 evidence remains immutable; negative, partial, blocked, and excluded results are not promoted.
- Sprint 80 is implementation and evidence work. Sprint 81 owns `1.0.0` publication.

## Atomic Tasks

Status legend: [ ] pending / [~] in progress / [x] done

### Phase A — Frozen diagnostic contracts

- [x] **80.1 — Freeze the representation-problem taxonomy.** Add one versioned machine-readable taxonomy for collapse/rank loss, anisotropy/inactive dimensions, redundancy/superposition, separability/probe leakage, density/OOD/distribution drift, sparse-feature instability, and sequence/trajectory drift where applicable. **Accept:** stable identifiers, applicability rules, required evidence, and unsupported-case behavior validate without model-specific code.
- [x] **80.2 — Freeze the benchmark-manifest schema.** Define one schema for model and revision, dataset and split, layer/slice axes, injected or known defect, metrics, seeds, controls, uncertainty, causal expectation, and pass/fail thresholds. **Accept:** malformed, post-hoc, or incomplete manifests fail closed.
- [x] **80.3 — Freeze the diagnostic-report schema.** Define versioned machine-readable fields for capture provenance, symptoms, localization, hypotheses, statistical evidence, interventions, comparisons, limitations, and next action. **Accept:** the schema distinguishes observation, explanation, causal result, and unsupported conclusion.
- [x] **80.4 — Implement the report validator.** Validate taxonomy references, evidence completeness, control outcomes, artifact hashes, and conclusion strength independently of report construction. **Accept:** missing controls, mismatched provenance, and overclaimed conclusions are rejected.
- [x] **80.5 — Predeclare the two core benchmark manifests.** Pin one encoder/autoencoder case and one transformer hidden-state case before workflow evidence is generated. **Accept:** revisions, data splits, defect/counterexample, thresholds, controls, expected localization, and causal success/falsification rules are immutable inputs.

### Phase B — High-level workflow substrate

- [x] **80.6 — Define the high-level diagnostic request/result API.** Introduce the smallest public configuration and result types needed to select captures, diagnostics, controls, interventions, comparisons, and output location. **Accept:** the API contains domain terms, no architecture-specific fields, and no duplicate configuration path.
- [x] **80.7 — Generalize capture selection and axis metadata.** Bind requested modules/layers and sample, slice, token, time, or checkpoint axes to existing capture, `LatentValue`, and `Trajectory` primitives. **Accept:** deterministic capture identity and complete provenance are preserved for both core models.
- [x] **80.8 — Implement the diagnostic workflow state machine.** Orchestrate capture → detect → localize → explain → intervene → compare → report without embedding method-specific algorithms in the coordinator. **Accept:** stages have explicit inputs/outputs, failure states, and resumable artifact boundaries.

### Phase C — Detection families

- [x] **80.9 — Integrate collapse, rank-loss, anisotropy, and inactive-dimension detection.** Reuse existing geometry/statistics primitives behind the taxonomy contract. **Accept:** positive/counterexample and negative controls separate real defects from benign low variance.
- [x] **80.10 — Integrate redundancy, superposition, separability, and probe-leakage detection.** **Accept:** leakage-safe splits, capacity controls, label randomization, and non-separable negatives prevent probe accuracy from becoming a diagnosis by itself.
- [x] **80.11 — Integrate density, OOD, and distribution-drift detection.** **Accept:** reference/test identity, calibration, uncertainty, shuffled controls, and distribution-free failure behavior are recorded.
- [x] **80.12 — Integrate sparse-feature and temporal-drift detection.** Reuse SAE/dictionary and trajectory primitives only where the representation exposes those axes. **Accept:** cross-seed feature stability and sequence controls gate the claim; non-applicable cases are omitted rather than passed.

### Phase D — Localization and explanatory validity

- [x] **80.13 — Localize findings across layers and dataset slices.** **Accept:** the workflow identifies the earliest affected layer and affected sample/slice under predeclared correctness criteria, not only a global score.
- [x] **80.14 — Localize findings across checkpoint, token, and time axes.** **Accept:** applicable axes preserve alignment and identity; absent axes are explicit non-applicability, not empty success.
- [x] **80.15 — Implement the statistical-control executor.** Centralize seeded repetitions, confidence intervals, null, shuffled, randomized, and cross-seed controls. **Accept:** failed controls block the associated diagnostic conclusion.
- [x] **80.16 — Integrate probe, TCAV, and Integrated-Gradients explanation evidence.** **Accept:** each method runs only for a declared hypothesis and reports fidelity, stability, selectivity, leakage checks, and uncertainty.
- [x] **80.17 — Integrate SAE, lens, geometry, density, and clustering explanation evidence.** **Accept:** feature labels or projections are never promoted without frozen stability/selectivity criteria and a declared relationship to the diagnosed symptom.

### Phase E — Causality, comparison, and reporting

- [x] **80.18 — Implement activation patching, ablation, and removal trials.** **Accept:** identity/zero-strength, random, shuffled, and off-target controls share the same captured inputs and downstream metric.
- [x] **80.19 — Implement steering and dose-response trials.** **Accept:** intervention strength, direction provenance, monotonic/non-monotonic response, random-direction behavior, off-target effects, and downstream task change are recorded.
- [x] **80.20 — Implement aligned checkpoint/run comparison.** **Accept:** comparisons require identical dataset slices, preprocessing, layer identity, seeds, and diagnostic configuration, and distinguish representation change from task-metric change.
- [x] **80.21 — Persist the content-addressed diagnostic artifact.** Reuse run-record and portable-artifact conventions. **Accept:** schema version, hashes, inputs, environment, seeds, intermediate stages, and validator result reproduce without hidden local state.
- [x] **80.22 — Render the concise AI-engineer report.** **Accept:** the report names symptom, location, evidence strength, causal support or falsification, limitations, and next engineering action; unsupported statements fail closed.

### Phase F — Core proof

- [x] **80.23 — Prove the encoder/autoencoder diagnosis end to end.** Use a new versioned encoder-defect manifest frozen before its proof run; preserve the original v1 and v2 manifests and their negative/inconclusive results as historical evidence. **Accept:** a controlled defect at the model or input-pipeline boundary (not solely an in-memory capture clamp), declared positive/counterexample and negative controls, and the single high-level detect → localize → explain → intervene → compare → report workflow pass with a fidelity-supported explanation, causal controls, a predeclared downstream task-utility comparison, and an independently validator-clean, rendered artifact. No threshold, target, or control may be revised after observing proof outcomes. **Evidence:** `artifacts/sprint-80/task-23.md`; `agent://Review80_23V3` — PASS.
- [x] **80.24 — Prove the transformer diagnosis end to end.** Execute the frozen leakage-safe transformer manifest through the same workflow. **Accept:** the same chain passes without a transformer-only reporting or orchestration path, and a versioned shared evidence contract separately validates real capture axes and content-addressed, sample-aligned target-label provenance before a validator-clean artifact and deterministic report are produced. Preserve the original frozen taxonomy and manifest and the historical fail-closed result. **Deep-review correction:** prospectively predeclare a stability threshold and evaluate it on an independent split/seeded replay not used to select that threshold; report the original explanation's stability as recorded but ungated until this separate evidence passes. **Evidence:** `artifacts/sprint-80/task-24.md`; `agent://Review80_24V2` — PASS for original v2 scope; `agent://Review80_24Stability` — PASS for prospective independent stability correction (0.974878 ≥ 0.8, validator-clean supplemental record).
- [x] **80.25 — Reproduce both core cases from a clean environment.** **Accept:** pinned inputs for the accepted prospective encoder v3 and transformer with versioned target evidence regenerate validator-clean run records and content-addressed artifacts with bounded runtime/resource evidence in a fresh non-editable install; a committed reproducible driver covers those accepted cases and pins their actual inputs. Historical failed v1 encoder and pre-amendment transformer outcomes remain distinct. Earlier accepted working-tree runs remain historical; revision `5241553` reproduced all three cases with 24/24 checks from an isolated non-editable checkout, byte-preserving Windows `core.autocrlf=true` audit, and current-source measurements. **Evidence:** `artifacts/sprint-80/task-25.md`; `agent://Review80_25Revision` — PASS on second deep-review G1/G2/G3 correction.
- [x] **80.26 — Publish the AI-engineer guide and executable examples.** **Accept:** a user starts from model plus data and reproduces both diagnoses without manually assembling low-level capture, statistics, intervention, comparison, or artifact plumbing. Behavior and provenance passed `agent://Review80_26Guide` and `agent://Review80_26Provenance`; third-review H2 publication commits `85a4c77`/`11ed10b` contain guide, both runnable examples and task evidence, with full clean-clone case executions. **Evidence:** `artifacts/sprint-80/task-26.md`; `agent://Review80_26Commit` — PASS.

### Phase G — Secondary proof and handoff

- [x] **80.27 — Resolve the bounded SmolVLA lane.** Verify checkpoint, dataset, license, and 16 GiB execution feasibility before running one secondary diagnostic proof. **Accept:** either a truthful bounded artifact passes or an explicit blocker remains; neither outcome gates the ordinary-DL core. **Evidence:** `artifacts/sprint-80/task-27.md`; `agent://Review80_27Blocked` — PASS on the explicit-blocker branch, not a SmolVLA diagnostic pass.
- [x] **80.28 — Run proportionate quality and compatibility gates.** The full gates (2,549 passed / 3 skipped / 42 deselected offline tests) were measured in the shared worktree at code basis `f15859b`, not a clean-clone suite. The API snapshot and H1 tool/test overlays were committed at `cef267e` and passed focused clean-clone checks (`agent://Review80_28Commit`). Fourth deep review D1's four live documentation conflicts were corrected at `e6c7754`, with revision-backed clean-clone snapshot, semantic inventory, link, and strict documentation checks recorded in `artifacts/sprint-80/task-28.md` at `96b3c07`; `agent://Review80_28Docs` — PASS. No clean-clone full-suite rerun is claimed.
- [x] **80.29 — Publish the Sprint 80 depth-evidence report.** Map supported claims to frozen manifests, reports, controls, interventions, clean reproductions, and review artifacts. **Accept:** preserve negative/excluded lanes and provide Sprint 81 a binary bounded-core readiness decision. **Report:** [Sprint 80 depth-evidence report](../SPRINT_80_DEPTH_EVIDENCE.md). **Handoff:** [task 80.29 evidence record](../../artifacts/sprint-80/task-29.md). Third deep review H2/H3 publication passed `agent://Review80_29Publish`; fourth deep D1/D2 global-plan/report/memory correction was committed at `4a5e8f8` with handoff `66f334f` and passed `agent://Review80_29FinalDocs`. Bounded core readiness remains conditional on the pending sprint-wide deep review; SmolVLA and release publication are excluded.

## Execution Order

1. Complete Phase A before generating benchmark evidence.
2. Complete the shared workflow substrate before detector- or model-specific integration.
3. Evidence-review each atomic task or explicitly tagged consecutive batch before advancing.
4. The encoder and transformer proofs may run in parallel only after their shared contracts and workflow dependencies pass.
5. Run the sprint-wide deep review only after all 29 task evidence gates pass.

Each completed task records a concise artifact summary under `artifacts/`, exact validation evidence, affected claims, negative results, and its evidence-review verdict.

## Stable-Depth Acceptance Gates

Sprint 80 passes only when all of the following are true:

1. **Capture:** supported core models expose deterministic, provenance-bound internal representations through the documented workflow.
2. **Detection:** each claimed representation-problem family has a non-trivial positive/counterexample benchmark and a negative/control case.
3. **Localization:** reports identify affected layers and relevant sample, slice, checkpoint, token, or time axes with predeclared correctness criteria.
4. **Statistical validity:** applicable results include uncertainty and leakage-safe randomized, shuffled, null, and cross-seed controls; a failed control blocks the claim.
5. **Explanatory validity:** projections and probes are not explanations by themselves; headline explanations pass frozen fidelity, stability, and selectivity gates.
6. **Causal validity:** at least one intervention confirms or falsifies each headline diagnosis with identity/zero-strength, dose-response, random, and off-target behavior where applicable.
7. **Reproducibility:** both core cases reproduce from a clean environment with pinned inputs and validator-clean, content-addressed artifacts.
8. **Usability:** both cases use the same high-level workflow and end in an actionable report that states what failed, where, evidence strength, causal result, limitations, and missing evidence.

## Non-Goals

- Maximizing historical D2/D3 theory-row percentages.
- Supporting every adapter, VLA, 3D representation, world model, or planner before 1.0.
- Treating visualization, import, checkpoint loading, or smoke execution as diagnostic evidence.
- Lowering thresholds or replacing failed controls after observing results.
- Requiring hardware above the approved 16 GiB release-evidence ceiling.
- Publishing `1.0.0`; Sprint 81 owns publication after the depth-evidence handoff passes.

## Notes / Blockers

Historical Sprint 79 evidence remains immutable. Existing TCAV significance, SAE cross-seed stability, steering randomized-control, and selected manifold/geometry failures remain blockers for only the corresponding diagnostic claims. The workflow must either resolve them under the frozen Sprint 80 contracts or omit those claims.

**Second deep review correction in progress (2026-09-25):** `agent://DeepReviewSprint80Final` failed on F1–F5; task-level corrections passed `agent://Review80_24Stability`, `agent://Review80_25Commit`, `agent://Review80_28Refresh`, and `agent://Review80_29Correct`. Second differential review `agent://DeepReviewSprint80Rerun` returned FAIL on G1–G5: accepted core absent from Git revisions, Git EOL conversion hazard for raw-byte-pinned evidence, clean replay preceding current source edits, and stale guide/global-plan status and guide leakage-gap provenance. Reopened 80.25, 80.26, 80.28, 80.29; earlier PASS scopes remain historical. No sprint-wide signoff yet; the earlier PASS_BLOCKED findings below remain historical, not permission to rewrite frozen inputs.

**Third deep review correction in progress (2026-09-25):** `agent://DeepReviewSprint80Third` returned FAIL on H1–H3. H1's API snapshot and five tool/test overlays are committed at `cef267e`, with focused clean-clone checks but no clean-clone full-suite rerun. This 80.29 publication adds the missing report, task records, quality summary, stability output, SmolVLA manifest, and small blocker evidence; it distinguishes the shared-worktree 80.28 gates at code basis `f15859b` from the later focused checks. Sprint 80 remains Active and a fresh deep review is pending; no sprint-wide signoff is asserted.

**Fourth deep review correction in progress (2026-09-25):** `agent://DeepReviewSprint80Fourth` returned FAIL on D1: committed API snapshot 214/211/8 at digest `3fd8c73e…` contradicts five live published docs still naming the obsolete 205/202/7 snapshot (two already corrected only in the worktree). Reopened 80.28 for API/reference/migration/README/changelog and 80.29 for global-plan consistency and revision publication. D2 notes uncommitted sprint decisions/lessons as non-gating until classified; 80.26 guide link will be checked against the corrected API reference, but its own published bytes and prior evidence gate remain unchanged. No sprint signoff yet.

**Final sprint-wide gate (2026-09-25):** The D1 live-doc/API inventory correction passed `agent://Review80_28Docs`; the D1 global-plan and D2 memory-scope correction passed `agent://Review80_29FinalDocs`. All 29 task rows are `[x]` and `agent://DeepReviewSprint80Fifth` returned **PASS** with no actionable findings at `8392b04`. Its low-severity, non-blocking observations concern unpublished early-task narrative summaries, historical commit-attribution shorthand, and dated pre-checkbox task records. The accepted scope is the two supported ordinary-DL cases and bounded Sprint 81 handoff, not general release or GPU/SmolVLA readiness; the 2,549-test suite remains shared-worktree evidence, not a clean-clone full-suite pass.

**80.23 blocker (recorded 2026-09-22, checkbox deliberately left `[ ]`):** the frozen encoder manifest's declared known defect is not observed on the pinned revision — under the frozen estimator the heldout bottleneck passes both thresholds (`bottleneck-effective-rank` 3.1149 ≥ 3.0, `bottleneck-singular-spread` 0.4735 ≥ 0.25; train, all-digits, and untrained-init mu captures pass as well), while every declared control case behaves as predeclared (counterexample scores above both thresholds, benign low-variance does not trigger, null recorded). Truthful localization is therefore negative, which leaves the 80.16 localization binding unresolvable and fails the seven-stage chain closed at `explain` — no validator-clean artifact or rendered report exists without fabricating a location — and no localization seam can express the manifest's expected feature-axis direction selection under the frozen effective-rank metric. Exact evidence, hashes, runtime, and commands: `artifacts/task_80.23_encoder_end_to_end_proof_summary.md` and `scripts/sprint80_task80_23_proof.py`. Resolution requires an evidence-review decision on the manifest's defect declaration, a predeclared injection protocol, and/or a feature-axis localization seam — never post-hoc tuning of the frozen inputs.

**80.23 acceptance amendment (user-selected 2026-09-23):** the original pinned ConvVAE v1 positive is not required to pass, because its frozen defect declaration was falsified. A prospective replacement proof satisfied the amended acceptance above; the frozen v1 `PASS_BLOCKED` and v2 capture-injection inconclusive outcomes remain immutable and may not be relabeled as passing. Evidence reviews `agent://Review80_23V2` and `agent://Review80_23Corrected` failed the former acceptance; the type-check finding was corrected and the new model-weight-lesion v3 proof passed `agent://Review80_23V3`.

**80.24 blocker (recorded 2026-09-22, checkbox deliberately left `[ ]`):** the frozen transformer manifest executed through the real seven-stage workflow passes its full scientific claim truthfully — pinned inputs verified byte-exactly (gpt2 commit `e7da7f22…`, wikitext selection reproduced against the pinned L04 manifest), grouped leakage-safe split, declared attribute separable at the frozen thresholds (`heldout-probe-accuracy` 1.0000 ≥ 0.7, `probe-leakage-gap` 0.4489 ≥ 0.15), all four manifest controls as declared, layer-axis localization naming the manifest-expected earliest layer `transformer.h.0`, supported probe explanation, supported concept-removal intervention with four passing controls, truthful `neither` comparison, byte-deterministic payloads and bit-identical full re-extraction — **but no validator-clean artifact or rendered report can exist**: the frozen taxonomy requires capture axes `{sample, feature, label}` for `separability_probe_leakage`, the frozen manifest declares only `{layer, token, slice}`, and the accepted 80.7 binder rejects undeclared axes and has no `label` axis at all, so the independent 80.4 validator refuses any truthful supported claim (`taxonomy applicability mismatch`) and 80.21 persistence fails closed with no files. Resolution requires an evidence-review decision touching a frozen contract (80.1/80.2/80.7), never a role alias or forged provenance. Exact evidence, hashes, runtime, and commands: `artifacts/task_80.24_transformer_hidden_state_proof_summary.md` and `scripts/sprint80_task80_24_proof.py`.

**80.24 contract amendment (user-selected 2026-09-23):** retain the frozen transformer v1 manifest and its scientific outcomes, but add a versioned generic evidence contract that does not misrepresent target labels as an activation-array axis. Bind the already declared target rule to sample identity and split provenance; the independent validator must validate both sources before accepting persistence. The original taxonomy and failed v1 persistence attempt remain immutable; no axis alias, invented labels, or transformer-only shortcut is authorized. The rule-bound proof passed `agent://Review80_24V2`; `artifacts/sprint-80/task-24.md` distinguishes the historical failure, preliminary v2 output, and final validator-clean artifact.

**80.25 blocker (recorded 2026-09-22, checkbox deliberately left `[ ]`):** a genuinely clean environment (fresh project root; `uv sync --frozen --no-dev --extra transformers --no-editable` from the declared `uv.lock`; non-editable install; fresh output/run-record/temp/datasets/modules caches; the declared gpt2 `e7da7f22…` and wikitext `f7762941…` snapshots hash-verified before and after) deterministically reproduces both frozen fail-closed outcomes with byte-identical stage payloads — encoder detect `faeff74e…`/localize `35c17b76…`, workflow fails closed at explain with the recorded `StageContractError`, `ACCEPTANCE: NOT PASSED`; transformer pooled `b0e60bc5…` re-extracted cold, detect `23863ec1…`/localize `1129ac27…`, all seven stages complete, persistence refuses on the recorded taxonomy/manifest/binder axes triangle (`taxonomy applicability mismatch for separability_probe_leakage`), `ACCEPTANCE: NOT PASSED` — therefore **validator-clean run records and content-addressed artifacts cannot be regenerated and the 80.25 acceptance is unreachable for the same frozen-contract reasons recorded for 80.23/80.24**. The attempt also surfaced and fixed (frozen content untouched) a non-frozen implementation defect that blocked installed-package execution entirely: schema/taxonomy lookup assumed the editable source layout (`<venv>/Lib/artifacts/…`); evidence in `artifacts/task_80.25_clean_environment_reproduction_summary.md`.

**80.25 clean reproduction (corrected and reviewed 2026-09-24):** the committed `scripts/sprint80_task80_25_clean_repro.py` reproduces accepted encoder v3, transformer v1 with target evidence v2, and the separate prospective transformer stability supplement in a fresh non-editable install under bounded resources; `artifacts/sprint-80/task-25.md` and `agent://Review80_25Commit` record 24/24 checks and validator-clean content-addressed outputs. The earlier temporary-driver run and its one-row randomized-control variance are historical (`agent://Review80_25Clean`); the committed replay returned the reference leakage gap. Historical old-v1 fail-closed reproductions above remain unchanged.

**80.26 blocker (recorded 2026-09-22, checkbox deliberately left `[ ]`):** the AI-engineer guide (`docs/AI_ENGINEER_GUIDE.md`, indexed as INDEX entry 29) and two minimal executable examples (`scripts/ai_engineer_example_encoder.py`, `scripts/ai_engineer_example_transformer.py`) are published and smoke-exercised: each drives only the high-level `DiagnosticRequest`/`DiagnosticWorkflow` plus one persistence call over the committed frozen compositions, and both reproduce the frozen outcomes with exit 0 (encoder: capture/detect/localize with real measurements — effective rank 3.1149, spread 0.4735, all controls passed/recorded, truthful negative localization, fail-closed `StageContractError` at explain, persistence refuses an incomplete run; transformer: all seven stages complete, report assembled, intervention supported at effect -0.2685, comparison `neither`, persistence refuses with `taxonomy applicability mismatch for separability_probe_leakage`). The literal acceptance — a user reproduces **both diagnoses** end to end — remains unreachable for the same frozen-contract reasons as 80.23/80.24/80.25: no validator-clean content-addressed artifact or rendered report can exist for either frozen manifest (the encoder chain never reaches `report`; the transformer report cannot pass the independent validator), so nothing is promoted and no success is claimed. Resolution requires an evidence-review decision on a frozen contract (80.1/80.2/80.7 or the encoder manifest/thresholds), never lowered thresholds or forged provenance. Evidence: `artifacts/task_80.26_ai_engineer_guide_examples_summary.md`.

**80.26 guide/examples (reviewed 2026-09-24):** both current accepted cases now execute from the published guide via runnable examples to validator-clean artifacts and deterministic rendered reports, with output-collision refusal and historical negative evidence retained. See `artifacts/sprint-80/task-26.md` and `agent://Review80_26Guide` — PASS.
**80.27 blocker (recorded 2026-09-23, checkbox deliberately left `[ ]`):** one bounded real CUDA proof executed at pushed SHA `2485071` from a disposable remote clone aborts fail-closed before any diagnosis — `AssertionError: captured matrix shape (64, 768) != (64, 1024)`: the real pinned policy (`lerobot/smolvla_libero@31d453f7`) emits per-camera `vision_context` captures of shape `(1024 tokens, 768 features)`, not the predeclared width 1024, and the frozen detector additionally refuses 64 samples at width 768 by contract (`undersampled batch: 64 samples cannot support rank estimation over 768 dims`, proven with synthetic input on the same box). Data stage passed (64 real frames, scanned 874, digest `0ee63f53…`); model blob sha `9a9f6413…` verified; no artifact persisted. Evidence: `artifacts/task_80.27_smolvla_secondary_lane_summary.md`. Secondary lane does not gate the core.

**80.27 bounded resolution (reviewed 2026-09-24):** checkpoint/data/license pins and the original real-CUDA failure remain grounded; the proposed versioned 768→32 representation is predeclared but unexecuted. A dirty local worktree blocked the mandatory clean-commit/push/disposable-clone remote sequence, and full-workflow 16-GiB peak feasibility is unmeasured. The task's explicit-blocker outcome passed `agent://Review80_27Blocked`; `artifacts/sprint-80/task-27.md` makes no SmolVLA diagnosis claim.

**80.28 blocker (evidence-reviewed PASS_BLOCKED 2026-09-23; checkbox deliberately left `[ ]`):** Current-candidate configured strict pyright and API gates pass. The 53-lane compatibility matrix had 52/53 exits 0; the single nonzero was the temporary wheel smoke calling `LatentValue` without its required `space` argument; fresh wheel rebuild/install plus corrected smoke passes. Full offline pytest remains 7 failed / 2490 passed / 5 skipped / 41 deselected (one candidate-only raw-byte schema pin mismatch, six tests blocked before assertions by absent optional `datasets`). Full release Ruff remains red only in three preserved proof/harness scripts (51 diagnostics; three format failures). Detailed evidence: `artifacts/sprint-80/task-28.md` and `agent://Review80_28Final`. Historical 80.25 remains PASS_BLOCKED; 80.23–80.27 statuses unchanged; no scientific claim was promoted.

**80.28 corrected candidate (reviewed 2026-09-25):** full Ruff check/format, configured strict Pyright, 2,549-test offline selection (3 skipped, 42 deselected), API snapshot, strict docs, evidence ledger, wheel build/fresh non-editable install, and current accepted-case pin verification passed (`artifacts/sprint-80/task-28.md`, `agent://Review80_28Refresh`). Clean-base imports and changed probe behavior passed on Python 3.12/3.13/3.14; the unchanged optional-extras matrix remains prior evidence, not a current rerun. The accepted committed 80.25 cold replay was not rerun after later source/style edits; 55 repository input pins, a 9/9 non-editable-install preflight, validators for all three persisted output roots, and byte-identical stability rendering passed. The GPT-2 checkpoint-backed replay remains excluded from the default offline lane as `large_download`; old red results above remain historical.

The first execution task is **80.1**. No product implementation begins before tasks 80.1-80.5 freeze the taxonomy, schemas, and core benchmark manifests.
