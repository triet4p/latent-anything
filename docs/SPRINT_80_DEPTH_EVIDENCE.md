# Sprint 80 Depth-Evidence Report

**Evidence cut:** 2026-09-25  
**Final sprint-wide review:** `agent://DeepReviewSprint80Fifth` — **PASS**, no actionable findings, at revision `8392b04` (2026-09-25).

**Sprint 80 diagnostic-depth gate:** **PASS — no unresolved blocker for the bounded supported ordinary-DL core.** This is not `1.0.0` publication approval.

**Scope:** Accepted prospective encoder v3 and transformer target-evidence-v2 cases; the separately gated transformer stability supplement; the fresh-root replay measured from source commit `5241553`; the byte-preserving Windows checkout; and 80.28 gate evidence measured in the shared worktree at code basis `f15859b`. That commit contains the 80.25 handoff, not the 80.28 handoff. The API snapshot/tool/test overlays and focused clean-clone checks are revision-backed at `cef267e`; the full suite was not rerun from a clean clone. The guide/examples are published by `85a4c77`/`11ed10b`; SmolVLA remains explicitly blocked and non-gating.

This correction preserves the earlier F1–F5 and G1–G5 findings and the third deep review's **FAIL** on H1–H3 without treating prior review passes as a final verdict. H1 is corrected by the revision-backed API snapshot/tool/test overlays at `cef267e`; H2/H3 were addressed by the publication package. A subsequent fourth deep review (`agent://DeepReviewSprint80Fourth`) returned **FAIL** on D1/D2; Task 80.29's narrow correction of the global plan and explicit memory-scope decision are recorded in [the task 80.29 handoff](../artifacts/sprint-80/task-29.md). A fresh final deep review remains pending, so this report is not final signoff or a deep-review PASS.

## Decision and interpretation

**Binary handoff: READY for Sprint 81 to proceed with the bounded ordinary-DL core demonstrated by the two accepted cases.** Evidence includes (1) the prospective model-bound encoder v3 lesion and independent artifact validation; (2) the transformer case with versioned target-evidence-v2 and independent validation; (3) the 24/24 committed fresh-root replay measured from source revision `5241553` (run `20260925-065327-115093-14640`) for both core cases and its separate stability supplement; (4) the byte-preserving Windows checkout audit; (5) the guide examples; and (6) the current 80.28 quality/compatibility gates and later handoff evidence at `f15859b`.

Here, **READY** means only that the two bounded, controlled diagnostic demonstrations are accepted as Sprint 81's ordinary-DL core reference. The final sprint-wide review is **PASS** (`agent://DeepReviewSprint80Fifth`; no actionable findings at `8392b04`), so the prior pending-review condition is closed. This does **not** mean that all stable-depth gates or theory coverage are complete, or that the package is generally release-ready for arbitrary models and datasets. The full cold replay remains measured from committed source revision `5241553`; the later `f15859b` and `cef267e` checks do not constitute a second full scientific replay. No full GPU/CUDA validation was performed. The encoder result is a prospective four-dimensional linear-autoencoder lesion, not a natural or production encoder failure; the transformer result is protocol-bounded separability, not proof of causal feature use.

Task-level reviews: `agent://Review80_23V3` PASS for encoder v3; `agent://Review80_24V2` PASS for the immutable target-evidence-v2 contract; `agent://Review80_24Stability` PASS for its separately frozen stability supplement; `agent://Review80_25Revision` PASS for the 24/24 replay and source/pin/Windows-checkout evidence; `agent://Review80_26Provenance` PASS for the bounded guide/examples; `agent://Review80_27Blocked` PASS on the explicitly blocked, non-gating SmolVLA branch; and `agent://Review80_28Refresh` plus `agent://Review80_28Docs` PASS for the scoped quality/API-documentation corrections. Earlier `agent://Review80_25Commit` and `agent://Review80_25Clean` apply to prior replay runs only. These task-level reviews are superseded as a sprint-wide decision by the final review cited above.

Status terms in this report are narrow:

- **Workflow/artifact PASS** means the named stages, controls, validation, and persistence completed for the cited frozen case.
- **Blocked / not run / not established** identifies an unmet or unexecuted claim; it is not a pass for that claim.
- **Historical negative** remains the outcome of its original run even where a later, separately frozen prospective case passes.

## Accepted evidence map

| Task | Current accepted evidence | Review and boundary |
|---|---|---|
| [80.23](../artifacts/sprint-80/task-23.md) | Prospective encoder v3 model-weight lesion; complete workflow, validator-clean artifact, deterministic rendered report. | `agent://Review80_23V3` PASS. Does not promote the historical ConvVAE v1 or capture-injection v2 outcomes. |
| [80.24](../artifacts/sprint-80/task-24.md) | Immutable v2 target-evidence artifact, whose original `coef_stability` threshold remains null, plus a separate predeclared real-GPT-2 grouped-split stability supplement (`coef_stability=0.974878 >= 0.8`) with independent record/report validation and deterministic rendering. | `agent://Review80_24V2` PASS for the v2 contract; `agent://Review80_24Stability` PASS for the separate supplement. The supplement is not a retroactive gate on the immutable v2 result; v1 persistence refusal remains historical. |
| [80.25](../artifacts/sprint-80/task-25.md) | Fresh-root replay of encoder v3, transformer target-v2, and the separate stability supplement re-measured from committed source revision `5241553`; 24/24 driver checks passed. The current proof-source hashes and 162-file source digest are recorded below. | `agent://Review80_25Revision` PASS. The `2026-09-24` replay and temporary-driver outputs remain historical; the temporary-driver leakage-gap variance is not attributed to the accepted revision-backed run. |
| [80.26](../artifacts/sprint-80/task-26.md) | English guide and two examples exercise the public high-level path and independently reload/validate/render each artifact. | `agent://Review80_26Provenance` PASS. The guide's conditional bounded-core readiness and run-specific metrics match the report; example smokes remain scoped to these pinned cases. |
| [80.27](../artifacts/sprint-80/task-27.md) | Explicit blocker branch for the secondary SmolVLA lane; prospective v2 declaration is frozen but unexecuted. | `agent://Review80_27Blocked` PASS on the blocker branch, not a diagnosis pass. This lane does not gate ordinary-DL core readiness. |
| [80.28](../artifacts/sprint-80/task-28.md) | The full quality-gate selection and broad gates were measured in the shared worktree at code basis `f15859b` and are documented in the now-published handoff; that commit contains the 80.25 handoff, not this 80.28 record. The API snapshot and five H1 overlays are revision-backed at `cef267e`, with focused clean-clone checks only. | The 2,549-test result is not a clean-clone full-suite run; optional-extras matrix results are prior evidence. No GPU/CUDA, network/model-acquisition, or SmolVLA diagnosis is established. |
| [80.29](../artifacts/sprint-80/task-29.md) / final gate | The report maps the supported bounded-core claims to their frozen evidence and preserves negative, blocked, and excluded lanes. | `agent://DeepReviewSprint80Fifth` — **PASS**, no actionable findings at `8392b04`; only low-severity, non-blocking observations remained. |

The durable task records above link the proof summaries, commands, logs, manifests, evidence bundles, and persisted outputs. The review artifacts are OMP records, not repository files.

The accepted independent Task 80.24 stability supplement is published as a separate [content-addressed run record](../artifacts/diagnostics/proof-80-24-stability-v1/runs/a95ccce32461b78ddd0197188fc1f4b51a1b349d434d3a9812dbcc8251dbeb2d.json) and [deterministic report](../artifacts/diagnostics/proof-80-24-stability-v1/reports/5f88ac98fe84ab80ed93dfc5a0e08a9972e2665649e152ecb2030503494135d0.md). Their raw SHA-256 values match their content-addressed filenames; the run record is 332,209 bytes. This is distinct from the later 80.25 replay measurement. The original v2 artifact's `threshold:null` stability observation remains ungated.

## Claim ledger: prospective encoder v3

**Supported claim:** On the prospectively frozen four-dimensional linear autoencoder and sklearn digits held-out split, the predeclared model-weight lesion is detected, localized, explained, causally reversed, compared with the healthy baseline, and persisted as a validator-clean diagnostic.

- **Frozen identity:** [v3 manifest](../artifacts/benchmark_manifest_sprint80_encoder_autoencoder_v3_model_weight_lesion.json), canonical commitment `f823efc1b016ca9ad62c0f207e17d138947040a39f5d1c1f42ab6d0d65f5954f`, raw SHA-256 `e5bcd341b1b746f05e4ac02f908bcd27f23bace39737c86c8ac38888e7451b82`; train-only [baseline checkpoint](../artifacts/benchmark_model_sprint80_encoder_autoencoder_v3_baseline.npz), SHA-256 `92829e57aaaebf52d5446d1a9c9db5de4e12c1657fad5ecc986329ed8752bb2a`. The frozen defect zeroes encoder weight row 0 and its bias in a model copy before held-out encoding; the baseline, decoder, other rows, and inputs stay unchanged. Resulting lesion checkpoint and lesion-record hashes are `81a29cc8d92b38164b108958764a6f1ffe07e5b822701077bd45faeffea91008` and `d1b0763ee791b0069dc94e13b698edb51f4fcd7711ff169ce22264c3130d0570`.
- **Detection and controls:** feature-variance ratio `0.0` (threshold `>= 0.1`), singular spread `0.0` (`>= 0.25`), and effective rank `2.9576990034956805` (`< 3.0`) support the declared lesion. The healthy counterexample, benign low-gain negative, and null-shuffle controls pass.
- **Localization and explanation:** localization selects exactly `brightness-axis-dim0` on the feature axis (confidence `1.0`, supported). The leakage-safe probe reports held-out accuracy `1.0` (threshold `>= 0.7`), coefficient stability `1.0` (`>= 0.8`), and leakage gap `0.9916666666666667` (`>= 0.15`); its capacity, randomized, and negative controls pass with disjoint train/evaluation identities.
- **Intervention and comparison:** restoring the lesioned row and bias raises held-out brightness-bin accuracy by `+0.4638888888888889`, above the frozen `0.02` tolerance; zero-strength, random-direction, shuffled-pairing, and off-target controls pass. The aligned healthy-versus-lesion comparison classifies `both`: representation variance-ratio delta `-0.8494925859561244` and task accuracy delta `-0.4638888888888889`, each beyond the predeclared `0.05` tolerance.
- **Persistence:** final proof run `6bca9afb72af32c4` is recorded in [`proof-run.json`](../artifacts/diagnostics/proof-80-23-v3-model-weight-lesion-complete-20260923/proof-run.json) and its [run record](../artifacts/diagnostics/proof-80-23-v3-model-weight-lesion-complete-20260923/runs/6bca9afb72af32c4.json). Artifact SHA-256 `e5c6db0ab5080807138e091bc332a5d3e788889a382917e93775b15f562aef0f`; report SHA-256 `8fb1528f39d355a311d2a8db39e9cf7db6455e1c7392915e21f377a05d13f194`; persisted [`encoder-diagnosis-v3.md`](../artifacts/diagnostics/proof-80-23-v3-model-weight-lesion-complete-20260923/encoder-diagnosis-v3.md) and deterministic render SHA-256 `b8d713de3181ef39990d994d4f3c708e584eb3c559f762376e2b9f2de65de6e3`. The independent validator passed.

**Historical encoder findings remain negative and separate.** The original frozen ConvVAE v1 manifest ([manifest](../artifacts/benchmark_manifest_sprint80_encoder_autoencoder_v1.json), commitment `b4c49c890cf34cc0530d499c2b82a09057f1f1ebf5b9d9a3843200760b137dd3`) did not exhibit its declared positive: held-out effective rank was `3.1149 >= 3.0`, singular spread `0.4735 >= 0.25`, and the truthful localization was negative. Its workflow stopped at explain because no supported location could be bound; persistence refused and wrote no artifact. The direct dim2 reconstruction preview (`-0.000028`) was not a completed workflow intervention and does not support the causal claim. The separate capture-injection v2 ([manifest](../artifacts/benchmark_manifest_sprint80_encoder_autoencoder_v2_injected.json)) remains an inconclusive capture-level result: explanation failed fidelity and identical replay comparison was `neither`. Neither historical result is relabeled by v3. See the [chronological encoder proof summary](../artifacts/task_80.23_encoder_end_to_end_proof_summary.md) and the v3 [task handoff](../artifacts/sprint-80/task-23.md).

The v3 evidence is restricted to a controlled, prospective lesion of the four-dimensional linear autoencoder on one digits brightness-bin task. It does not establish naturally occurring collapse in the historical ConvVAE, production encoder defects, general digit-recognition utility, GPU behavior, or arbitrary encoder/dataset generality.

## Claim ledger: transformer with target-evidence-v2

**Supported claim:** On the pinned GPT-2/WikiText-2 selection, the predeclared section-header attribute is separable under the frozen leakage-safe probe protocol, localized first at `transformer.h.0`, and supported by the declared removal intervention. The v2 target-evidence contract permits a truthful, independently validated persisted report without pretending labels are activation axes.

**Prospective stability supplement:** The immutable v2 artifact's recorded `coef_stability` has `threshold:null`; its recorded `passed` status is not a threshold-gated measurement and is not counted toward this gate. A distinct protocol froze `coef_stability >= 0.8` before the seed-83 grouped-split outcome. The separate real-GPT-2 run passed independent record/report validation and deterministic rendering: held-out accuracy `1.0000`, coefficient stability `0.974878` (`>= 0.8`), and leakage gap `0.425490` (`>= 0.15`); all declared controls passed across five grouped training-subset refits. The clean 80.25 replay repeated this supplement in a separate output root. It uses the same selected 2,048 WikiText validation rows with a new grouped split/fresh probe seeds, not an independent source corpus, and supports separability/stability only—not causal feature use.

- **Frozen identity and target provenance:** [Transformer manifest](../artifacts/benchmark_manifest_sprint80_transformer_hidden_state_v1.json), `sprint80-core-transformer-hidden-state-probe-v1`, commitment `c4773238ddf4d8854cb40ad26811b7b204cab8210ff9fe45f79e83e12da4675a`, pins GPT-2 commit `e7da7f221d5bf496a48136c0cd264e630fe9fcc8` and WikiText validation revision `f776294184f13b8ff2337b3841cf9269a6216d1e`. Versioned `diagnostic-evidence-v2` binds target record `target-section-header-attribute-record`, SHA-256 `3e470ef055e2a546408a6f51717bbc5a4d8bf2478410555862045a516955bc19`, and predeclared section-header rule SHA-256 `9b12485635decf738c4904702f75d7d5ae8cb6663555e2af5ee66476269f025b`. It aligns 2,048 sample IDs and labels with the 1,549/499 grouped train/evaluation split. Real activation axes remain `slice` and `feature`; target labels are separate provenance, not forged capture axes.
- **Detection, controls, explanation:** In rule-bound run `421578bb1c06fa15` and revision-backed replay run `c64d382b1560e40e` from source commit `5241553`, held-out accuracy was `1.0000` (threshold `0.7`) and leakage gap was `0.44889779559118237` (threshold `0.15`); capacity, label-randomization, non-separable-negative, and split-swap controls behaved as declared. The one-row-higher `0.45090180360721444` belongs only to the historical temporary-driver run and is separately identified below. Localization identifies `transformer.h.0`; probe evidence is supported.
- **Intervention and comparison:** removing the section-header direction at `transformer.h.0` changes probe accuracy `1.0000 → 0.7315` (effect `-0.2685`); zero-strength, random, shuffled, and off-target controls pass. Aligned replay comparison is `neither`, with both declared metric deltas `0.000000`; it does not claim task improvement.
- **Persistence:** final rule-bound run `421578bb1c06fa15` is recorded in the [run record](../artifacts/diagnostics/proof-80-24-v2-rule-bound/runs/421578bb1c06fa15.json). Content-addressed artifact SHA-256 `e2ebd582ac9fa0defe03f1b221290067d931a9b99b606b661da75fccab7eee7a`; diagnostic-report SHA-256 `c670acea7b27665581844916f17fb03aa38b5aa6d7740583dea34a5a6f467bdb`; deterministic rendered [`diagnostic-report`](../artifacts/diagnostics/proof-80-24-v2-rule-bound/diagnostic-report) SHA-256 `788b9a316ff3ec14922b7a1515fd8aee2b0d2b4b4d0dc884f321eb0e68b8693a`. The report records `diagnostic-evidence-v2` and validator `passed`; all 13 registered evidence links were rehashed. The separate target record is persisted at [`artifacts/3e470...`](../artifacts/diagnostics/proof-80-24-v2-rule-bound/artifacts/3e470ef055e2a546408a6f51717bbc5a4d8bf2478410555862045a516955bc19).

**Historical transformer v1 persistence failure remains real.** The earlier v1 run completed its seven scientific workflow stages, but persistence failed closed: taxonomy `separability_probe_leakage` required capture axes `{sample, feature, label}`, while the frozen manifest declared `{layer, token, slice}` and the binder could not truthfully bind `sample` or `label`. The independent validator refused the report and wrote no artifact. The accepted current result uses the separate v2 target-evidence contract; it does not rewrite that old failure or add a fictional label axis. The final task-24 handoff and [proof summary](../artifacts/task_80.24_transformer_hidden_state_proof_summary.md) preserve both records.

The detected text attribute is separable under this protocol; it is not evidence that GPT-2 causally uses section headers. Row length and last-token identity remain correlates/confounds documented in the proof report. No result is claimed for other layers/axes, target rules, models, or datasets.

## Clean reproduction, variance, and user-facing path

The accepted Task 80.25 replay ran on 2026-09-25 from committed source revision `5241553`, using `uv run python scripts/sprint80_task80_25_clean_repro.py --hf-root F:/llms/hf/models`. Run `20260925-065327-115093-14640` recorded `PASSED`, 24 checks, zero failures, unchanged pre/post pins and external snapshots, independent validation of all three cases, byte-exact output copies, and verified cleanup. The proof run's evidence was added in `938edd9`; the latest Task 80.25 handoff is recorded at `f15859b`. These are distinct provenance roles: `5241553` is the measured source revision, while `f15859b` is the later handoff/evidence revision. The fresh environment used a non-editable install (`uv sync --frozen --no-dev --extra transformers --no-editable`), the pinned `datasets==3.6.0` / `huggingface-hub==0.35.3` overlay, isolated proof caches, Python 3.13.3, and no Hugging Face auth token. Each proof was capped at 1,800 seconds and 16 GiB of process-tree RSS.

| Case | Run / evidence | Wall / peak process-tree RSS |
|---|---|---|
| Encoder v3 | `fa9bfd738a7dbc01`; artifact SHA-256 `b2db02d6b9f39dc2331d503018b0f964a93e3e29414303c112652ac08e9ec0cb` | 7.272 s / 353,763,328 B |
| Transformer v1 + target evidence v2 | `c64d382b1560e40e`; artifact SHA-256 `b8a4afdc17108e14ebf028b469b0e32fd519b63acdfb5aea8cd91b26de3f488d` | 944.193 s / 1,840,766,976 B |
| Transformer stability supplement (separate run) | Run-record SHA-256 `111fb5726b0f338eadd15d250d70470bb8e7922355e8192acb70f9294fc844fe` | 1,005.243 s / 1,692,811,264 B |

The transformer run reproduced pooled-state SHA-256 `b0e60bc54412e50e917ec8b9dfb3c4c2af467b691b27953e9a5feb5d1021a33b`, held-out accuracy `1.0`, and leakage gap `0.44889779559118237` (95% interval `[0.40881763527054105, 0.49699398797595196]`; threshold `>=0.15`). Its removal intervention changed accuracy `1.0000 → 0.7315` (effect `-0.2685`); the aligned replay comparison is `neither`, with zero metric deltas. The distinct stability supplement measured held-out accuracy `1.0`, coefficient stability `0.9748782295192386` (`>=0.8`), and leakage gap `0.4254901960784314` (`>=0.15`); the v2 artifact's own `threshold:null` stability field remains ungated. For encoder v3, restoration intervention effect is `+0.4638888888888889`; the separate candidate-minus-baseline comparison delta is `-0.4638888888888889`.

See the corrected [80.25 handoff](../artifacts/sprint-80/task-25.md), [replay summary](../artifacts/task_80.25_evidence_committed_20260925-065327-115093-14640/summary.json), [run records](../artifacts/task_80.25_evidence_committed_20260925-065327-115093-14640/runs.json), and [pins](../artifacts/task_80.25_evidence_committed_20260925-065327-115093-14640/pins.json). The 80.25 handoff records the encoder-v3 proof source SHA-256 `87891b0c75d4ddcfe890792e421ac4108801d08aa49a9d3a234da764481cb53f`, transformer target-v2 source `dee2d016bf9a22dfd5ef9089ca7bea74bc28bf86d6f36f78ae28db505f8d6f7a`, stability proof source `a1c08fff6cb45f8a768ae57d2f727a4a89392b04f0b3bd26f4807e0972dc8ac5`, shared probe module `0d114dfeb884c5ddbbabbee9f077ac284e547936a249423b65f517b456c75f93`, and 162-file `src/latent_anything` tree digest `3226dc26ea19e15889e29f70c33c83ac29a0cf66115a81bd229a3b8eace75be4`. The replay pins 54 explicit repository files plus that source-tree digest (55 current input commitments).

**Windows byte-preservation and content-addressed evidence:** At measured revision `5241553`, a disposable checkout with `core.autocrlf=true` matched all 54/54 explicit file pins and the 162-file tree digest; 15/15 transformer content-addressed blobs retained filename-equals-SHA-256. The later `f15859b` checkout audit and task-28 evidence verified the current driver pins and summary bytes in a Windows checkout, byte-identical output trees (encoder 19 files, transformer 17, stability 2), and all 14/14 encoder plus 15/15 transformer content-addressed blobs. The 80.28 audit recorded the scoped `-text` attributes and raw bytes; its 9/9 current dry-runs in the worktree and `core.autocrlf=true` clone are preflight/checkout checks, not replacements for the committed 24/24 replay above. See the [80.28 handoff](../artifacts/sprint-80/task-28.md) and `agent://Review80_28Git`.

The historical temporary-driver replay observed leakage gap `0.45090180360721444` (95% interval `[0.4108216432865731, 0.49699398797595196]`), one evaluation row out of 499 (`0.002004008016`) above the accepted `0.44889779559118237`. It used uncommitted inline `python -c` proof execution and remains historical variance, not the accepted revision-backed replay. The earlier 2026-09-24 committed run, the temporary-driver run, the first failed finalization, and all original negative runs remain separately recorded; none were rewritten as successes or overwritten. The [80.24 handoff](../artifacts/sprint-80/task-24.md) records the rule-bound reference; the [AI-engineer guide](AI_ENGINEER_GUIDE.md) and [encoder](../scripts/ai_engineer_example_encoder.py)/[transformer](../scripts/ai_engineer_example_transformer.py) examples carry the same current measurement provenance. Task 80.26's reviewed smokes independently reloaded, validated, and deterministically rendered these two bounded cases; they do not establish a general-purpose auto-diagnoser.

## Secondary lane: SmolVLA explicitly blocked and non-gating

The [80.27 handoff](../artifacts/sprint-80/task-27.md) and [lane summary](../artifacts/task_80.27_smolvla_secondary_lane_summary.md) preserve one historical real CUDA capture attempt, not a completed diagnosis. On the pinned policy, capture shape was `(64, 768)` while the frozen v1 proof asserted `(64, 1024)`; independently, the estimator cannot support 768 dimensions from 64 samples. The workflow stopped before detect/localize/explain/intervene/compare/report; no validator-clean artifact or SmolVLA diagnosis is supported. The exact small [remote capture log](../artifacts/diagnostics/80-27-smolvla-v1-remote-capture.log) and [64×768 detector probe](../artifacts/diagnostics/80-27-smolvla-v1-dim768-probe.json) are published with the recorded SHA-256 values.

A prospective [v2 manifest](../artifacts/benchmark_manifest_sprint80_smolvla_secondary_v2.json), canonical SHA-256 `ec70d423fcf33a6ebbaabfb2dccbcac6ee8de167f53a125bc377386f6cd1cf1f`, freezes a 768→32 projection but remains a schema-valid, unexecuted specification, not proof. A second remote run was not attempted because the pre-run checkout was dirty and the mandatory clean-commit/push/disposable-clone remote-CUDA workflow requires stopping on a dirty worktree. No new remote execution, CUDA diagnosis, corrected-workflow peak memory, or 16-GiB full-workflow feasibility is claimed. This explicit blocker branch is accepted and does not gate the ordinary-DL core.

## Current quality and compatibility gate disposition (80.28)

The [current task record](../artifacts/sprint-80/task-28.md) and [gate summary](../artifacts/task_80.28_quality_compatibility_gates_summary.md) describe the current candidate, distinct from the older red evidence cut.

| Gate | Current observed result | Boundary |
|---|---|---|
| Strict Pyright | PASS — 0 errors, 0 warnings, 0 information diagnostics in the 80.28 shared-worktree gate. | Not rerun in the clean clone; the later clean-clone checks were focused and are recorded at `cef267e`. |
| Ruff and formatting | PASS — full shared-worktree `ruff check src tests scripts`; `ruff format --check src tests scripts` reported **509 files** already formatted. | Not a clean-clone full-repository gate; no network or GPU execution is implied. |
| Focused proof/pin regressions | PASS — 7 driver tests, 25 probe tests, 55 repository-input checks, and 9/9 driver preflight were recorded for the shared-worktree candidate; the replay independently measured all three cases at source revision `5241553` with 24/24 checks. At `cef267e`, a fresh clone passed the API check, 12 focused tests, file-scoped Ruff checks, and 9/9 driver dry-run. | The `5241553` measurements, shared-worktree checks at `f15859b`, and focused clean-clone checks at `cef267e` are distinct evidence. No clean-clone 80.28 full-suite run is claimed. |
| Final offline pytest selection | PASS — **2,549 passed, 3 skipped, 42 deselected, 39 warnings** in 2,562.53 s using `not network and not large_download and not viz and not integration`; measured in the shared worktree at code basis `f15859b`. | Not rerun as a full suite in a clean clone. Excluded categories are not passes. |
| API snapshot and evidence ledger | API snapshot PASS — SHA-256 `3fd8c73e6fd9fa9bae107b4be6727cc3e9c3c907c9b9d757fdae4c267d8691ec`; `--check` passed in the clean clone at `cef267e`. Evidence-ledger validator PASS — 107 capabilities, core 41/63 (65.1%), overall 41/64 (64.1%) in the shared-worktree gate. | Ledger counts are capability coverage, not depth-proof outcomes; the two checks were not both rerun in the clean clone. |
| Docs and packaging | PASS — strict MkDocs, wheel/source archive build, fresh non-editable wheel install, and corrected smoke were recorded in the shared-worktree gate. | This report's strict docs build is independently recorded for the publication clone below; no clean-clone wheel/packaging run is implied. |
| Compatibility matrix | **Prior evidence, not rerun for 80.28** — base-import isolation 3/3 and optional-extra resolution/import 36/36 across Python 3.12.12, 3.13.3, 3.14.0; VQ 11 passed each on 3.12/3.13; LeRobot CPU 22 passed/1 skipped each; diffusion 8 passed/1 skipped each. | `lerobot-smolvla` was import-smoked only; no SmolVLA diagnosis or GPU proof. |

**Earlier red chronology is retained, not erased:** the previous 2026-09-23 report recorded 51 Ruff diagnostics plus 3 formatting failures in three preserved proof scripts and an offline-suite result of 7 failures / 2,490 passes / 5 skips / 41 deselections. The first 80.28 current-candidate offline run separately had 3 failures (Windows PowerShell BOM in raw stdin, CRLF-versus-LF schema pin bytes, and the unavailable pretrained GPT-2 snapshot); after corrections an intermediate run had 1 unrelated CLI optional-distribution-metadata fixture failure. The final current offline run passed after the focused fixes, and the missing-checkpoint replay was excluded as `large_download`. The initial `52/53` compatibility runner count also included a throwaway wheel-smoke invocation missing a required argument; the corrected smoke passed. Those initial failures remain history, not current gate status and not scientific passes.

The offline suite does not include network, `large_download`, `viz`, or integration tests. The pinned GPT-2 replay checkpoint was absent from the offline cache; no model acquisition or remote CUDA test was run. SmolVLA's extra was only resolution/import-smoked. These omissions are explicit.

## Stable-depth gate interpretation and excluded claims

| Gate for the bounded supported core | Evidence-based disposition |
|---|---|
| Capture and provenance | PASS for the v3 lesion and v2 target-evidence transformer cases with frozen identities and truthful capture/target records. |
| Detection and controls | PASS for the declared v3 lesion and transformer separability target under their frozen thresholds and controls; not a general detector benchmark. |
| Localization | PASS for v3 `brightness-axis-dim0` and transformer `transformer.h.0`; the historical ConvVAE v1 remains truthfully unlocalized. |
| Explanation | PASS for encoder v3's declared fidelity/stability/selectivity gates and for the separate transformer stability supplement (`coef_stability=0.974878 >= 0.8` across five grouped training subsets on seed 83). The original transformer v2 `threshold:null` observation is recorded but not gated or counted; this supplemental threshold applies only to its separately frozen protocol. Transformer target association is not causal feature-use evidence. |
| Causal evidence and comparison | PASS for the applicable declared controls and binary v3 restoration / transformer removal interventions; the transformer aligned replay comparison remains `neither`. Each core workflow uses a single intervention strength, so dose-response is **not applicable**; no dose-response measurement is claimed for either accepted core case. |
| Persistence and reproducibility | PASS for validator-clean original outputs and the 24/24 committed 80.25 fresh-root replay re-measured from source revision `5241553`, including the separate stability supplement. The later `f15859b` Windows checkout audit, current pins, dry-run, and output-tree/CAS checks corroborate byte preservation; they are not described as a second cold replay. |
| Usability | PASS for the published guide/examples reproducing the two bounded cases and independently validating their outputs. |
| Quality/compatibility | **Scoped PASS evidence:** the full selected suite and broad-gate results are shared-worktree measurements; the API snapshot, 12 focused tests, file-scoped Ruff checks, and 9/9 preflight are clean-clone evidence at `cef267e`. The clean clone did not rerun the full 2,549-test suite or complete broad matrix; excluded lanes remain unverified. |

No breadth claim is supported for arbitrary models/datasets, production encoder failures, detector families beyond these cases, GPU/CUDA performance, or deployment settings. These results do not establish TCAV, Integrated Gradients, SAE feature stability, lens/geometry/clustering, density/OOD/drift, sparse-feature, temporal/trajectory, steering, or SmolVLA scientific validity. Existing theory-row evidence and its claim-specific blockers remain governed by their own sources.

## Sprint 81 handoff and status

**Decision: PASS — Sprint 80 has no unresolved depth blocker for the bounded supported ordinary-DL core.** The final review signs off the specific accepted claims and boundaries in the evidence map and gate table above: prospective encoder v3, transformer target-evidence-v2, the separately gated stability supplement, their 24/24 fresh-root replay, and the published guide/examples. SmolVLA remains explicitly blocked and non-gating; GPU/CUDA, arbitrary-model/dataset breadth, and other excluded methods are not passes. Sprint 81's separate release-quality and publication gates remain open; this handoff authorizes no tag or publication.

Earlier sprint-wide reviews `agent://DeepReviewSprint80Third` (H1–H3) and `agent://DeepReviewSprint80Fourth` (D1/D2) returned **FAIL**; their corrections were subsequently checked by task-level reviews, including `agent://Review80_28Docs` and `agent://Review80_29FinalDocs`. The final differential review `agent://DeepReviewSprint80Fifth` then returned **PASS** with no actionable findings at `8392b04`. It recorded only low-severity, non-blocking observations about early-task summary publication scope, historical commit-attribution shorthand, and dated pre-checkbox task records. These observations do not invalidate the accepted evidence or reopen the bounded depth gate. Task 80.29's [handoff](../artifacts/sprint-80/task-29.md) retains the detailed publication and revision audit.

The [docs index entry 30](INDEX.md) already points to this report and remains correct. Main owns the Sprint 80 plan status; this report neither changes nor snapshots task 80.29's checkbox.
